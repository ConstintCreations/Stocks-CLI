[package]
name = "stocks_cli"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12", features = ["json", "blocking"] }
serde = { version = "1", features = ["derive"] }
dotenv = { version = "0.15" }
clap = "4.6.0"
